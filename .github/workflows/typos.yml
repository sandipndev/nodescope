name: spelling

on:
  pull_request:
    branches: [ main ]

jobs:
  spelling:
    name: spell check with typos
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: crate-ci/typos@master
        with:
          config: typos.toml
